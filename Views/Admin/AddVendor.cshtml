@{
    Layout = "~/Views/Shared/_Admin_Layout.cshtml";
    ViewData["Title"] = "Add New Vendor";
}

<style>
    /* Container for the form card */
    .form-card-container {
        max-width: 800px;
        margin: 30px auto;
    }

    .custom-form-card {
        background: #ffffff;
        border-radius: 12px;
        border: 1px solid #e0e0e0;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        overflow: hidden;
    }

    /* Professional Header */
    .form-header {
        background: #ffffff;
        padding: 25px 35px;
        border-bottom: 1px solid #f1f1f1;
        display: flex;
        align-items: center;
        gap: 15px;
    }

        .form-header i {
            font-size: 1.5rem;
            color: #d32f2f; /* Nexus Red */
        }

        .form-header h4 {
            color: #1a237e; /* Navy Blue */
            font-weight: 700;
            margin: 0;
        }

    /* Form Body */
    .form-body {
        padding: 35px;
    }

    .form-label {
        color: #5f6368;
        font-weight: 600;
        font-size: 0.88rem;
        margin-bottom: 8px;
        display: block;
    }

    .form-control {
        border: 1px solid #ced4da;
        border-radius: 8px;
        padding: 12px 15px;
        font-size: 0.95rem;
        background-color: #fcfcfc;
        transition: all 0.2s ease;
    }

        .form-control:focus {
            border-color: #d32f2f;
            background-color: #fff;
            box-shadow: 0 0 0 4px rgba(211, 47, 47, 0.1);
            outline: none;
        }

    textarea.form-control {
        min-height: 100px;
        resize: none;
    }

    /* Layout Grid */
    .form-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 25px;
        margin-bottom: 20px;
    }

    /* Icon Styling */
    .input-group-text {
        background-color: #ffffff;
        border-right: none;
        color: #d32f2f;
    }

    .form-control.with-icon {
        border-left: none;
    }

    /* Button Styling */
    .button-row {
        display: flex;
        gap: 15px;
        margin-top: 30px;
    }

    .btn-save-vendor {
        flex: 2;
        background: #d32f2f;
        color: white;
        border: none;
        padding: 14px;
        border-radius: 8px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: 0.3s;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
    }

        .btn-save-vendor:hover {
            background: #b71c1c;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(211, 47, 47, 0.3);
        }

    .btn-cancel {
        flex: 1;
        background: #f8f9fa;
        color: #5f6368;
        border: 1px solid #ddd;
        padding: 14px;
        border-radius: 8px;
        font-weight: 600;
        text-align: center;
        text-decoration: none;
        transition: 0.2s;
    }

        .btn-cancel:hover {
            background: #e9ecef;
            color: #333;
        }

    @@media (max-width: 768px) {
        .form-grid

    {
        grid-template-columns: 1fr;
    }

    .button-row {
        flex-direction: column;
    }

    }
</style>

<div class="form-card-container">
    <div class="custom-form-card">
        <div class="form-header">
            <i class="fa-solid fa-truck-ramp-box"></i>
            <div>
                <h4>Add New Vendor</h4>
                <p class="text-muted small mb-0">Register a business partner or equipment supplier</p>
            </div>
        </div>

        <div class="form-body">
            <form method="post">
                <div class="form-grid">
                    <div class="form-group">
                        <label class="form-label">Vendor Business Name</label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="fa-solid fa-building"></i></span>
                            <input name="VendorName" class="form-control with-icon" placeholder="e.g. Cisco Systems" required />
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="form-label">Primary Contact Number</label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="fa-solid fa-phone-volume"></i></span>
                            <input name="ContactNo" type="tel" class="form-control with-icon" placeholder="e.g. +92 312 4567890" />
                        </div>
                    </div>
                </div>

                <div class="form-group">
                    <label class="form-label">Official Office Address</label>
                    <div class="input-group">
                        <span class="input-group-text" style="align-items: flex-start; padding-top: 15px;">
                            <i class="fa-solid fa-location-dot"></i>
                        </span>
                        <textarea name="Address" class="form-control with-icon" placeholder="Provide complete office or warehouse location details..."></textarea>
                    </div>
                </div>

                <div class="button-row">
                    <button type="submit" class="btn-save-vendor">
                        <i class="fa-solid fa-paper-plane"></i> Save Vendor Profile
                    </button>
                    <a href="/Admin/Vendors" class="btn-cancel">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>